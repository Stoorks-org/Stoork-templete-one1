import { SearchAdminPipe } from './search-admin.pipe';

describe('SearchAdminPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAdminPipe();
    expect(pipe).toBeTruthy();
  });
});
